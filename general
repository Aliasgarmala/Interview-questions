Integer to different formats

int x = 100;
System.out.println(Integer.toBinaryString(x));
Integer.toString(100,8) // prints 144 --octal representation

Integer.toString(100,2) // prints 1100100 --binary representation

Integer.toString(100,16) //prints 64 --Hex representation

HashSet doesn't have a get method. So, if you want to do a get do contains and if true then i.e the get method

String.valueOf(sum - 2) // convert int to string
Integer.parseInt(string) // convert string to int

Arrays.asList(arr) // converting arrays as list (in arraylist or L.L)

System.out.println(Arrays.toString(arr)); // to directly print the array

String str = str1.replaceAll("\\s+",""); //remove all the whitespaces

In string questions see if you can split the words using spaces and make an array and then solve it

Immutable classes are simpler and more efficient i.e. have less # of setters
 
https://docs.oracle.com/javase/tutorial/essential/exceptions/tryResourceClose.html
